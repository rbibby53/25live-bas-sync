# Institution-specific config & data (copy the .example files and edit).
# These hold each site's hosts, accounts, defaults, and room list — keep local.
config.yaml
defaults.yaml
space_mapping.yaml

# Docker secrets (passwords for docker compose) — never commit.
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Editor backups written by editor.py on save
*.bak

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db
